"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{getSession}from"@/lib/api";export default function AuthGuard({children}:{children:React.ReactNode}){const r=useRouter(),[ready,setReady]=useState(false);useEffect(()=>{if(!getSession()){r.replace("/login");return}setReady(true)},[r]);return ready?<>{children}</>:<div className="screen-loader">Loading Homeowner360…</div>}
