"use client";import Link from"next/link";import{useEffect,useState}from"react";import{getPayments}from"@/lib/api";import type{Payment}from"@/lib/types";const money=(n:number)=>new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD'}).format(n);export default function Payments(){const[i,setI]=useState<Payment[]>([]),[e,setE]=useState('');useEffect(()=>{getPayments().then(setI).catch(x=>setE(x instanceof Error?x.message:'Unable to load payments'))},[]);return <><div className="page-header"><div><h1>Payments</h1><p>Review payment activity across your portfolio.</p></div></div>{e&&<div className="error-box">{e}</div>}<div className="panel"><div className="table-wrap"><table><thead><tr><th>Payment</th><th>Loan</th><th>Date</th><th>Amount</th><th>Status</th><th/></tr></thead><tbody>{i.map(x=><tr key={x.paymentId}><td>#{x.paymentId}</td><td>{x.loanId}</td><td>{new Date(x.paymentDate).toLocaleDateString()}</td><td className="money">{money(x.amount)}</td><td><span className="badge badge-success">{x.status}</span></td><td><Link href={`/loans/${x.loanId}`} className="btn btn-secondary">View loan</Link></td></tr>)}</tbody></table></div></div></>}
