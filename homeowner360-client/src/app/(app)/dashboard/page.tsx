"use client";import{useEffect,useState}from"react";import StatCard from"@/components/StatCard";import{getDashboard,getLoans}from"@/lib/api";import type{Dashboard,Loan}from"@/lib/types";const money=(n:number)=>new Intl.NumberFormat('en-CA',{style:'currency',currency:'CAD',maximumFractionDigits:0}).format(n);export default function Dashboard(){const[d,setD]=useState<Dashboard|null>(null),[l,setL]=useState<Loan[]>([]),[e,setE]=useState('');useEffect(()=>{Promise.all([getDashboard(),getLoans()]).then(([a,b])=>{setD(a);setL(b.slice(0,5))}).catch(x=>setE(x instanceof Error?x.message:'Unable to load dashboard'))},[]);return <><div className="page-header"><div><h1>Dashboard</h1><p>Overview of your mortgage servicing portfolio.</p></div></div>{e&&<div className="error-box">{e}</div>}{!d?<div className="loading">Loading dashboard…</div>:<><div className="stats-grid"><StatCard label="Customers" value={String(d.totalCustomers)} detail="Customer records" icon="♙"/><StatCard label="Loans" value={String(d.totalLoans)} detail="Loans in portfolio" icon="▣"/><StatCard label="Original loan amount" value={money(d.totalLoanAmount)} detail="Funded principal" icon="$"/><StatCard label="Outstanding balance" value={money(d.totalOutstandingBalance)} detail="Current balance" icon="◔"/><StatCard label="Payments" value={money(d.totalPayments)} detail="Total payment value" icon="↗"/><StatCard label="Payment count" value={String(d.totalPaymentsCount)} detail="Recorded payments" icon="#"/></div><div className="panel"><div className="panel-header"><h2>Recent loans</h2></div><div className="table-wrap"><table><thead><tr><th>Loan</th><th>Customer</th><th>Original</th><th>Balance</th><th>Rate</th></tr></thead><tbody>{l.map(x=><tr key={x.loanId}><td><strong>{x.loanNumber}</strong></td><td>{x.customerId}</td><td className="money">{money(x.originalAmount)}</td><td className="money">{money(x.currentBalance)}</td><td>{x.interestRate.toFixed(2)}%</td></tr>)}</tbody></table></div></div></>}</>}
